// src/components/icons.js
import React from 'react';
import { 
  Home, 
  Users, 
  Leaf, 
  ClipboardList, 
  DollarSign, 
  MapPin, 
  Archive, 
  LogOut, 
  PlusCircle, 
  Edit, 
  Trash2, 
  Eye, 
  EyeOff 
} from 'lucide-react';

// Using React.createElement for better compatibility
export const HomeIcon = (props) => React.createElement(Home, props);
export const UsersIcon = (props) => React.createElement(Users, props);
export const LeafIcon = (props) => React.createElement(Leaf, props);
export const ClipboardListIcon = (props) => React.createElement(ClipboardList, props);
export const DollarSignIcon = (props) => React.createElement(DollarSign, props);
export const MapPinIcon = (props) => React.createElement(MapPin, props);
export const ArchiveIcon = (props) => React.createElement(Archive, props);
export const LogOutIcon = (props) => React.createElement(LogOut, props);
export const PlusCircleIcon = (props) => React.createElement(PlusCircle, props);
export const EditIcon = (props) => React.createElement(Edit, props);
export const Trash2Icon = (props) => React.createElement(Trash2, props);
export const EyeIcon = (props) => React.createElement(Eye, props);
export const EyeOffIcon = (props) => React.createElement(EyeOff, props);